import { Routes } from '@angular/router';
import { CalculatorComponent } from '@app/features/investment/calculator/calculator.component';

export const investmentRoutes: Routes = [{ path: '', component: CalculatorComponent }];
