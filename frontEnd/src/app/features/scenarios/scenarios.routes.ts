import { Routes } from '@angular/router';
import { ScenariosComponent } from '@app/features/scenarios/scenarios.component';

export const scenariosRoutes: Routes = [{ path: '', component: ScenariosComponent }];
